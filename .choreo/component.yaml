schemaVersion: 1.1

configurations:
  env:
    - name: BOT_TOKEN
      valueFrom:
        configForm:
          displayName: Discord Bot Token
          required: true
          type: secret

build:
  runtime: python
  command: python3 app.py
